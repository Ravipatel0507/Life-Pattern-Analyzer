services:
  - type: web
    name: life-pattern-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn life_pattern_analyzer:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
